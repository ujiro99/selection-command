export function importIf(targetMode: string, path: string): void {
  // This function is a placeholder for the macro and will be replaced by the macro provider.
  // It should not be called directly in the code.
  throw new Error(
    `importIf macro should not be called directly. It is a compile-time macro. ${targetMode} ${path}`,
  )
}
