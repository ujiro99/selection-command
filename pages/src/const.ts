export enum OPEN_MODE {
  POPUP = 'popup',
  WINDOW = 'window',
  TAB = 'tab',
}

export enum SPACE_ENCODING {
  PLUS = 'plus',
  PERCENT = 'percent',
}

export enum SORT_ORDER {
  searchUrl = 'searchUrl',
  title = 'title',
  download = 'download',
  star = 'star',
  addedAt = 'addedAt',
}
